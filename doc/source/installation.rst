============
Installation
============

At the command line::

    $ pip install oslo.storage

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv oslo.storage
    $ pip install oslo.storage